# SMTP Configuration
MAIL_HOST=smtp-relay.brevo.com # Replace with your SMTP host (e.g., smtp-relay.brevo.com for Brevo, in-v3.mailjet.com for Mailjet)
MAIL_PORT=587 # SMTP port (587 for TLS, 465 for SSL)
MAIL_USERNAME=your_username_or_api_key # For Brevo, use your username. For Mailjet, use your public API key.
MAIL_PASSWORD=your_password_or_private_key # For Brevo, use your password. For Mailjet, use your private API key.
MAIL_SECURE=false # Use "true" for SSL, "false" for TLS (string, not boolean)

# Email Configuration
MAIL_FROM=your_email@example.com # Optional: The default "from" email address

# Template Configuration
TEMPLATE_PATH=src/core/helpers/mails/ # Optional: Path to your email templates